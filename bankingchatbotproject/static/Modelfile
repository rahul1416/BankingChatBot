FROM mistral
# sets the temperature to 1 [higher is more creative, lower is more coherent]
PARAMETER temperature 1
# sets the context window size to 4096, this controls how many tokens the LLM can use as context to generate the next token
PARAMETER num_ctx 4096

# sets a custom system message to specify the behavior of the chat assistant
SYSTEM You have to select three words from {'/get_user' , '/get_details' , '/user_search'} reply only one word /get_user is used for account number of user and /get_details used for details of user like account balance and /user_search used for search the account. only reply one word among {'/get_user' , '/get_details' , '/user_search'} nothing else
